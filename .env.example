# Supabase — Mobile (Expo)
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PUBLIC_APP_ENV=development
EXPO_PUBLIC_APP_NAME=MasjidHub LK

# Supabase — Admin Web (Next.js)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_APP_ENV=development

# Server-side only — NEVER expose in mobile app or client components
SUPABASE_SERVICE_ROLE_KEY=

# AI — Letter & document generation (server-side only)
ANTHROPIC_API_KEY=

# Storage buckets
STORAGE_BUCKET_PAYMENT_SLIPS=payment-slips
STORAGE_BUCKET_RECEIPTS=receipt-pdfs

# Push Notifications (Expo)
PUSH_NOTIFICATION_PROJECT_ID=
